[package]
name = "bootstrappable_buddy_allocator"
version = "0.1.0"
edition = "2024"
license = "MIT"

[dependencies]
binary_search_tree = { git = "https://github.com/an-owl/binary_search_tree" }
memory_addresses = "0.3.0"

[dev-dependencies]
rand = "0.10.0"